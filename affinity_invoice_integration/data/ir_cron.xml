<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <data noupdate="1">
        <record id="ir_cron_fetch_glowsims_vouchers_daily" model="ir.cron">
            <field name="name">GlowSIMS: Fetch Previous Day Vouchers</field>
            <field name="model_id" ref="model_voucher_fetch_wizard"/>
            <field name="state">code</field>
            <field name="code">model.cron_fetch_previous_day_vouchers()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall"
                   eval="(datetime.now().replace(hour=4, minute=0, second=0) + timedelta(days=1 if datetime.now().hour >= 4 else 0)).strftime('%Y-%m-%d %H:%M:%S')"/>
            <field name="active">True</field>
        </record>
    </data>
</odoo>