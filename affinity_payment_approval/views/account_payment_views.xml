<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <data>
        <record id="view_account_payment_search_inherit_approval" model="ir.ui.view">
            <field name="name">account.payment.search.inherit.approval</field>
            <field name="model">account.payment</field>
            <field name="inherit_id" ref="account.view_account_payment_search"/>
            <field name="arch" type="xml">
                <xpath expr="//search" position="inside">
                    <filter name="needs_review" string="Needs my Review"
                            domain="[('reviewer_ids', 'in', uid), ('state', 'not in', ['posted', 'cancel'])]"
                            help="My Payments to review"/>
                    <filter name="tier_validated" string="Validated" domain="[('validated', '=', True)]"
                            help="Payments validated and ready to be confirmed"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>